# execution service
# @HELP signal <host> x <commandline>

# @TODO need an output convention describing how to encode non-output date into line. Example: return value, output handle.
eval $@ 2>&1

#awk "{ print \"$outputcontrol\"
#       print
#     }"

# moved to dispatch
#   sed "s/^/$HOSTNAME: /"
# doesn't alway keep hostname and output line in sync
#awk "{ 
#       printf \"$HOSTNAME\" \": \" >\"/dev/stderr\"
#       printf \$1
#     }"
